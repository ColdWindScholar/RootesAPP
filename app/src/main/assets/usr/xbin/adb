export HOME="$Data_Dir"
arch=$(uname -m)
lu="$HOME/.android"
     if [[ ! -d "$lu" ]]; then
        mkdir -p "$lu"
        chown $APP_USER_ID:$APP_USER_ID "$lu"
     fi

$nativeLibPath/libadb.so "$@"
